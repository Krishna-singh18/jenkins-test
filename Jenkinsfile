pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code cloned successfully'
            }
        }

        stage('Build') {
            steps {
                sh 'pwd'
                sh 'ls -la'
                echo 'Build Successful'
            }
        }
    }
}